<div id="category">
    {% if taxon.parent is not empty and not taxon.parent.isRoot() and taxon.parent.enabled %}
        <a href="{{ path('sylius_shop_product_index', {'slug': taxon.parent.slug, '_locale': taxon.parent.translation.locale}) }}" class="btnRetour" {{ sylius_test_html_attribute('vertical-menu-go-level-up') }} title="{{ 'sylius.ui.go_level_up'|trans }}">
            <svg class="svg-icon" role="img"><use xlink:href="/chullanka/assets/img/sprites.svg#ico-fleche-back"/></svg>
        </a>
    {% endif %}
    <h1>{{ taxon.name }}</h1>
    {% if taxon.description %}
    {{ taxon.description|excerpt(50, '#description') }}
    {% endif %}
</div>

{#
<ul class="ctaCats">
    <li><a href="#">Chaussures randonnées femme</a></li>
    <li><a href="#">Chaussures randonnées homme</a></li>
</ul>
#}
<div id="news">
    <p>&nbsp;</p>
    {#
    <h2>Voici les Nouveautés Chullanka</h2>
    <div class="prodList">
        <div class="list">
            <div><?php include('inc/inc-prodinlist.php'); ?></div>
            <div><?php include('inc/inc-prodinlist.php'); ?></div>
            <div><div class="prodInList">
                    <a href="#"><img src="//picsum.photos/330/467?random=44" alt="" class="round5 guide"></a>
                </div>
            </div></div>
        </div>
        <div role="tablist" class="dots"></div>
        <script>
            window.addEventListener('load', function(){
                new Glider(document.querySelector('#news .list'), { 
                    itemWidth: 330,
                    slidesToShow: 1,
                    slidesToScroll : 1,
                    duration: 2,
                    draggable: true,
                    scrollLock: true,
                    dots: '.dots',
                    responsive: [
                        {
                            breakpoint: 1024,
                            settings: {
                                slidesToShow: 3
                            }
                        }
                    ]
                });
            });
        </script>
    </div>
    #}
</div>
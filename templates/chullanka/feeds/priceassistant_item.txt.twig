{{ item.title|e }};{{ item.brand|e }};{{ item.id|e }};{{ item.univers|e }};;{{ item.taxExclPrice|e }};;;{{ item.taxPercent|e }};EUROS;{{ item.gtin|e }};{{ item.code|e }};{{ item.link|e }};{{ item.imageLink|e }};{{ item.qty|e }}
